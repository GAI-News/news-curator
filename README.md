# GAI News Curator

## Dev Installation

Create a virtual environment as desired, and then:

```commandline
git clone git@github.com:GAI-News/news-curator.git && cd news-curator
pip install -r requirements.xt
```


